@code text

| 小问            | 口诀            | 展开含义                                       |
| ------------- | ------------- | ------------------------------------------ |
| **a) COMMIT** | **“写锁记持”**    | **写**入日志 → **锁**释放 → 状态**记**账（可见）→ **持**久化 |
| **b) 为什么并发**  | **“吞时硬稳”**    | **吞**吐高 **时**延低 **硬**件吃满 数据**稳**           |
| **c) 丢失更新**   | **“两读两写丢前写”** | 两事务**读**同值 → 各自计算 → 两次**写** → **前写丢**（被覆盖） |

@end
