import java.util.Map;

import javax.net.ssl.SSLKeyException;

import ArrayList.ArrayList;
import Linked.LinkedListUseExample;
import Comparable.FaceExample;
import Stack.StackExample;
import Map.MapExample;

import Linked.UsingExample;

import Queue.LinkedQueue;

import ArraySet.ArraySetExample;
import reviewComparator.Person;
import reviewComparator.PersonExample;

public class Main {
    public static void main(String[] args) {
        // // Calculator evaluationCalculation = new Calculator("({[]})");
        // // StackExample se = new StackExample();
        // MapExample me = new MapExample(
        // "This is a test, and all the things are shit, when there's nothing's a new,
        // we fall apart");
        // FaceExample fe = new FaceExample();
        // // UsingExample<String> linkedNodeUsingExample = new UsingExample<String>();
        // LinkedListUseExample<String> LE = new LinkedListUseExample<>();
        // LinkedQueue<String> q = new LinkedQueue<String>();
        // q.offer("shit1");
        // q.offer("shit2");
        // while (!q.isEmpty()) {
        // System.out.println(q.poll());
        // }
        // ArraySetExample asE = new ArraySetExample();
        PersonExample pe = new PersonExample();
    }
}
